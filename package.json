{
  "name": "myanmar-2d-calculator",
  "version": "1.0.0",
  "description": "Myanmar 2D Lottery Calculator with commission and payout tracking",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp index.html manifest.json www/ && cp -r assets www/ 2>/dev/null || true",
    "android": "npx cap open android",
    "sync": "npm run build && npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor/filesystem": "^7.0.0",
    "@capacitor/share": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
